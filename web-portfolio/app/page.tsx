// This page will be handled by middleware for language detection
// The middleware will redirect to the appropriate language route

export default function RootPage() {
  // This component won't render as middleware will redirect
  return null
}
